// This function converts temperature Celcius into degrees Fahrenheit
// S is O(1)
// T is O(1)

/*const toFahrenheit = (degreeCelcius: number): number => {
    return 1.8 * degreeCelcius + 32;
}

// This function logs all numbers in an array in a loop, in linear time.
// S is O(n)
// T is O(n), space and time depends upon the size of the array input
const logNums = (nums: number[]) => {
    for(let i=0; i < nums.length; i++){
        console.log(nums[i])
    }
}

// Log an array of number pairs (Quadratic Time)
// i.e. arr [1. 2. 3] => 1,1 1,2 1,3 2,1 2,2 2,3 3,1 3,2 3,3
//O (n^2) or O(n*n) space complexity
const logPairs = (nums: number[])=> }
    for(let i=0; i < nums.length; i++){
        for(let j=0; j < nums.length; j++){
            console.log(i,j)
        }
    }

    */